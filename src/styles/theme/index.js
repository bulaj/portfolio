const fontFamily = {
    primary: '"Open Sans", "Arial", "sans-serif"',
    heading: '"Raleway", "Arial", "sans-serif"',
};

const fontSize = {
    normal: 16,
};

const colors = {
    primary: '#066295',
    secondary: '#fff',
};

const weights = {
    extraLight: '200',
    light: '300',
    normal: '400',
    medium: '500',
    semibold: '600',
    bold: '700',
};

const spacing = {
    normal: 8,
    big: 16,
};

const theme = {
    fontFamily,
    fontSize,
    weights,
    colors,
    spacing,
};

export default theme;
